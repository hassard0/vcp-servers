{
  "name": "@vcp/gateway",
  "version": "0.1.0",
  "description": "VCP enforcing Gateway: manifest verification, policy, proof-bound grants, taint engine, attestation, audit, and end-to-end invoke.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@vcp/sdk": "0.1.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test \"test/*.test.ts\""
  }
}
